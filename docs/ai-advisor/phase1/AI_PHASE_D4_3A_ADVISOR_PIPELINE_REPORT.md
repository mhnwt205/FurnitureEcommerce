# D4.3a — Advisor Pipeline Artifacts

The existing advisor body is now exposed as `getAdvisorPipelineArtifacts`; public `getAdvisorResponse` maps its final recommendation artifact back to the unchanged public response. Retrieval metadata, enriched candidates, eligibility diagnostics and selected candidates are internal only. No clarification policy/state/API/frontend behavior is enabled.
