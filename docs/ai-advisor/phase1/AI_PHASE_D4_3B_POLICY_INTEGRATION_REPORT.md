# D4.3b — Policy Integration

Conversation now invokes the advisor artifact pipeline once, builds a Candidate Summary from existing artifacts, and derives internal clarification state. Public response remains the recommendation result. Summary/policy failures retain the legacy recommendation response. This is transitional: ranking/writer still run even when the internal decision is clarify.
