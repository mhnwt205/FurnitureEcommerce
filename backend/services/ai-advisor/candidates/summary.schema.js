import { z } from 'zod';
import { AI_CATEGORIES, AI_COLORS, AI_MATERIALS } from '../intent/intent.taxonomy.js';
export const noResultReasons = ['no_category_match', 'no_budget_match', 'no_attribute_match', 'excluded_only', 'out_of_stock_only', 'no_active_product', 'no_cheaper_match', 'no_more_expensive_match', 'no_different_product', 'no_smaller_match', 'no_larger_match', 'missing_comparative_reference', 'unknown'];
export const candidateFilterDiagnosticsSchema = z.object({
  beforeBudgetCount:z.number().int().nonnegative(), afterBudgetCount:z.number().int().nonnegative(), beforeAttributeCount:z.number().int().nonnegative(), afterAttributeCount:z.number().int().nonnegative(), beforeExclusionCount:z.number().int().nonnegative(), afterExclusionCount:z.number().int().nonnegative(), beforeStockCount:z.number().int().nonnegative(), afterStockCount:z.number().int().nonnegative(), exclusionApplied:z.boolean(), stockRequired:z.boolean(),
  beforeComparativeCount:z.number().int().nonnegative().default(0), afterComparativeCount:z.number().int().nonnegative().default(0), comparativeApplied:z.boolean().default(false), comparativeType:z.string().nullable().default(null)
}).strict();
export const candidateSummarySchema = z.object({ primaryCount:z.number().int().nonnegative(), retrievedCount:z.number().int().nonnegative(), eligibleCount:z.number().int().nonnegative(), fallbackUsed:z.boolean(), fallbackReason:z.enum(['none','primary_empty_category_fallback','primary_empty_keyword_fallback','primary_empty_all_active_fallback','unknown']), noResultReasons:z.array(z.enum(noResultReasons)).max(5), categoryMatched:z.boolean(), budgetMatched:z.boolean().nullable(), attributeMatched:z.boolean().nullable(), exclusionMatched:z.boolean().nullable(), stockMatched:z.boolean().nullable(), comparativeMatched:z.boolean().nullable().default(null), comparativeType:z.string().nullable().default(null), minEffectivePrice:z.number().int().nonnegative().nullable(), maxEffectivePrice:z.number().int().nonnegative().nullable(), availableCategories:z.array(z.enum(AI_CATEGORIES)).max(10), availableColors:z.array(z.enum(AI_COLORS)).max(10), availableMaterials:z.array(z.enum(AI_MATERIALS)).max(10) }).strict().superRefine((v,c)=>{if(v.minEffectivePrice!==null&&v.maxEffectivePrice!==null&&v.minEffectivePrice>v.maxEffectivePrice)c.addIssue({code:'custom',message:'min price exceeds max'});});
