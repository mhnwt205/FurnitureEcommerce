import assert from 'node:assert/strict';import test from 'node:test';import { buildRelaxationProposal } from '../services/ai-advisor/relaxation/policy.service.js';
const session={id:'12345678',turnCount:1,intent:{budget:{max:10_000_000}},fieldMeta:{colors:{strength:'required'}},excluded:{colors:['white'],materials:[],categories:[],styles:[]}};
test('relaxation proposals are deterministic, bounded and never created for missing reference',()=>{const a=buildRelaxationProposal({reasonCode:'no_budget_match',session,candidatePriceDiagnostics:{nearestEffectivePrice:12_000_000}});const later=buildRelaxationProposal({reasonCode:'no_budget_match',session:{...session,turnCount:2},candidatePriceDiagnostics:{nearestEffectivePrice:12_000_000}});assert.equal(a.options[0].value,12_000_000);assert.equal(a.options.length<=3,true);assert.equal(a.proposalId,later.proposalId);assert.equal(buildRelaxationProposal({reasonCode:'missing_comparative_reference',session}),null);assert.equal(buildRelaxationProposal({reasonCode:'excluded_only',session}).options[0].type,'clear_exclusion');});
